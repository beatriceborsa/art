<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/actionbar_opacity</item>
        <item name="colorPrimaryDark">@color/actionbar_opacity</item>
        <item name="colorAccent">@color/actionbar_opacity</item>
        <item name="titleTextColor">@color/actionbar_opacity</item>
        <item name="tabIndicatorColor">@color/actionbar_opacity</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Colore scritte interne al menu setting. -->
    <style
        name="ActionBar.Solid.Ribbit.TitleTextStyle"
        parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/actionbar_opacity</item>
    </style>

</resources>
